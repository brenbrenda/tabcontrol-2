//
//  account.swift
//  tabcontrol
//
//  Created by chia on 2019/12/10.
//  Copyright © 2019 chia. All rights reserved.
//

import Foundation
/*
struct Account {
    let user:String
    let password:String
    
    enum SerializationError:Error {
        case missing(String)
        case invalid(String, Any)
    }
    init(json:[String:Any]) throws {
        guard let user = json["帳號"] as? String else {throw SerializationError.missing("account is missing")}
        guard let password = json["密碼"] as? String else {throw SerializationError.missing("password is missing")}
        self.user = user
        self.password = password
    }
    static int basePath = "120.125.78.203"
}*/
